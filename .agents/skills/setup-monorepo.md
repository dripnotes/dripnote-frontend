# 모노레포 설정 (Setup Monorepo)

- 패키지 매니저로는 반드시 `pnpm`을 사용합니다.
- 내부 패키지들 간의 연결은 `workspace:*` 프로토콜을 사용해 링크합니다.
- 의존성(Dependencies)은 루트(최상단)에서 공통으로 관리하거나, 필요에 따라 필터(`--filter`)를 이용해 각 개별 앱에 정확히 맞추어 관리합니다.
